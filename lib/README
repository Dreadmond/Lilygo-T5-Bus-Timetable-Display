
This directory is intended for project specific (private) libraries.
PlatformIO will compile them into the project.

Libraries placed here are automatically added to the include path
and built along with the project source files.

